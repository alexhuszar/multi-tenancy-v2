# tenants

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build tenants` to build the library.

## Running unit tests

Run `nx test tenants` to execute the unit tests via [Jest](https://jestjs.io).
