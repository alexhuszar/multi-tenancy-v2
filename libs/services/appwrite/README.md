# appwrite

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build appwrite` to build the library.

## Running unit tests

Run `nx test appwrite` to execute the unit tests via [Jest](https://jestjs.io).
